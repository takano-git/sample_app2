<section class="hero is-success">
  <div class="hero-body">
    <div class="container">
      <h1 class="title">
        ダイアリー詳細
      </h1>
    </div>
  </div>
</section>

<section class="section">
  <div class="container">
    <div class="columns is-centered">
      <div class="column is-7">
        <div class="card">
          <div class="card-image">
            <figure class="image is-4by3">
              <%= attachment_image_tag @diary, :image %>
            </figure>
          </div>
          <div class="card-content">
            <div class="media">
              <div class="media-content">
                <p class="title is-4"><%= @diary.title%></p>
              </div>
            </div>
            <div class="content">
              <table class="table is-narrow">
                <tr>
                  <th>日記</th>
                </tr>
                <tr>
                  <td><%= simple_format @diary.body %></td>
                </tr>
              </table>
              <% if @diary.user.id == current_user.id %>
                <%= link_to "編集", edit_diary_path(@diary), class: "button is-success" %>
              <% end %>
            </div>
          </div>
        </div>
      </div>

      <div class="column is-4">
        <article class="panel is-link">
          <p class="panel-heading">
            By <%= @diary.user.username %>
          </p>
          <div class="panel-block">
            <p class="control">
              <%= @diary.user.profile %>
            </p>
          </div>

          <%= link_to user_path(@diary.user), class: "panel-block" do %>
            <span class="panel-icon">
              <i class="fas fa-user" aria-hidden="true"></i>
            </span>
              <%= @diary.user.username %>さんのページへ
          <% end %>
        </article>
      </div>
    </div>
  </div>
</section>
